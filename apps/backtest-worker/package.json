{
  "name": "@cryptox/backtest-worker",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node ../../scripts/start-worker.mjs",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nestjs/common": "^11.2.3",
    "@nestjs/core": "^11.2.3",
    "pg": "^8.23.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  }
}
