{
  "name": "@cryptox/contracts",
  "private": true,
  "exports": {
    "./rest": { "types": "./rest/index.ts", "import": "./rest/index.ts", "require": "./dist/packages/contracts/rest/index.js" },
    "./websocket": { "types": "./websocket/index.ts", "import": "./websocket/index.ts", "require": "./dist/packages/contracts/websocket/index.js" }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.contract-tests.json",
    "lint": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.contract-tests.json"
  }
}
