import { describe, expect, it } from "vitest";
import { listStrategies } from "./index";
describe("strategy skeleton", () => { it("stubs the runtime API", () => { expect(() => listStrategies()).toThrow("NOT_IMPLEMENTED"); }); });
