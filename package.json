{
  "name": "Cryptox",
  "version": "1.0.0",
  "description": "Strategy cryto platform",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "dev:backend": "turbo run dev --filter=backend",
    "dev:worker": "turbo run dev --filter=backtest-worker",
    "dev:frontend": "turbo run dev --filter=frontend",
    "migrate": "pnpm --filter @cryptox/infra migrate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.22.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "dependency-cruiser": "^18.2.0",
    "turbo": "^2.10.9",
    "typescript": "^7.0.2"
  }
}
